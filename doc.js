 <button onClick={()=>HanelHelpFunction(item)}>

this is good thng for when react render the comp  the function not wokring again so this is the best way for handel  this 
if you type this  <button onClick={()=>HanelHelpFunction(item)}>    like this 
 <button onClick={HanelHelpFunction(item)}>  +++ 
